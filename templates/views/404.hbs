<!DOCTYPE html>
    <head>
        <meta charset="utf-8">
        <title>Page not Found</title>

        <link rel="stylesheet" href="css/styles.css">
    </head>
    <body>
        {{>headers}}
        {{errorMessage}}
        {{>footer}}
    </body>
</html>